# Frontend environment (Vite).
# Local dev needs NO env file — the dev server proxies /api to localhost:3001.
# Set this only for production builds (Netlify): the backend's public URL.
VITE_API_URL=https://your-backend.up.railway.app
